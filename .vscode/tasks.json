{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Build & Run walk (production)",
            "command": "make && ./bin/walk 0",
            "options": {
                "cwd": "${workspaceFolder}/Pages-src/scripts/build"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        },
        {
            "type": "shell",
            "label": "Build & Run walk (development)",
            "command": "make && ./bin/walk",
            "options": {
                "cwd": "${workspaceFolder}/Pages-src/scripts/build"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}