{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Build & Run walk",
            "command": "make && ./bin/walk",
            "options": {
                "cwd": "${workspaceFolder}/Pages-src/scripts/build"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}